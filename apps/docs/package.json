{
  "name": "@semajsx/docs",
  "version": "0.1.0",
  "description": "SemaJSX documentation site",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun run build.tsx",
    "dev": "bun run build.tsx --watch",
    "clean": "rm -rf dist .ssg-state.json",
    "serve": "bun run --bun vite preview --outDir dist"
  },
  "dependencies": {
    "@semajsx/ssg": "workspace:*",
    "@semajsx/ssr": "workspace:*",
    "@semajsx/dom": "workspace:*",
    "@semajsx/core": "workspace:*",
    "@semajsx/signal": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@semajsx/configs": "workspace:*",
    "typescript": "^5.9.3",
    "vite": "^7.0.0"
  }
}
