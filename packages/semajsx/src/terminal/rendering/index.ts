/**
 * Terminal rendering helpers
 *
 * Internal rendering functions for different terminal element types.
 * These are low-level utilities used by the renderer, not user-facing components.
 */

export { renderBorder, renderBackground } from "./box";
export { renderTextNode, renderTextElement } from "./text";
